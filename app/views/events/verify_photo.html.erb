<div class=:"container">
<div class="contentarea container">
	<h1 id="title">Sign in!</h1>

	<div class="flash-message">
	  <% if flash[:notice] %>
	    <h2><%= flash[:notice] %></h2>
	  <% end %>
	</div>
	<p>
    <div id="allput" class="container">
	   </p>
      <div class="camera">
        <video id="video">Video stream not available.</video>
        <button id="startbutton">Take photo</button>
      </div>
      <canvas id="canvas">
      </canvas>
      <div class="output">
        <img id="photo" alt="The screen capture will appear in this box.">
      </div>
	   <p>
    </div>
	</p>
</div>
<%= form_with(url: post_photo_path, local: true) do |form| %>
<%= hidden_field_tag 'img', nil, id: 'img_field' %>

<div class="actions">
	<%= form.submit "Click to Check In" , class:"waves-effect waves-light btn z-depth-4", style:"border-radius:2em;" %>
</div>
<% end %>
</div>
